# CleanShed — График уборки в общежитии

PWA-приложение для автоматического составления и управления графиком уборки в блоке общежития. Генерирует справедливое распределение нагрузки между жильцами с учётом личных и общих комнат.

---

## Описание

Приложение предназначено для блоков общежития, где несколько жильцов делят общие помещения (кухня, коридор, ванная) и каждый также отвечает за уборку своей личной комнаты. Система автоматически составляет график на несколько недель вперёд, распределяя нагрузку максимально справедливо.

### Возможности

**Пользовательский интерфейс:**
- Просмотр задач на сегодня с отметкой выполнения
- Полный график уборки, разбитый по неделям
- Статистика уборок за последние 3 месяца
- Тёмная тема с переключателем в хедере (выбор сохраняется)
- Адаптивный дизайн, работа на мобильных устройствах
- PWA — установка на домашний экран

**Управление (админ-панель):**
- Добавление, редактирование и удаление жильцов
- Добавление, редактирование и удаление мест уборки
- Назначение доступных личных мест для каждого жильца
- Генерация графика на нужное количество недель
- Просмотр и сброс мастер-пароля

**Алгоритм генерации:**
- Все места есть в каждом дне графика, личные — обязательно
- Личные комнаты назначаются только их владельцам
- Человек за день убирает что-то одно: либо личное, либо общее
- Никто не убирает то же место, что на прошлой неделе (если есть альтернатива)
- Нагрузка выравнивается счётчиками внутри текущей генерации, при равенстве — случайный выбор

---

## Технологии

| Компонент | Технология |
|-----------|-----------|
| Backend | Kotlin + Spring Boot 4.1.1 |
| Frontend | Vue.js 3 (CDN) |
| База данных | SQLite |
| ORM | Hibernate (Spring Data JPA) |
| Безопасность | Spring Security |
| PWA | Service Worker + Web App Manifest |

---

## Требования

- **Java** 26+
- **Gradle** 8.5+

---

## Установка и запуск

### Быстрый старт

```bash
# Клонирование репозитория
git clone <repository-url>
cd cleanshed

# Запуск (первый раз скачает зависимости)
./gradlew bootRun
```

Приложение будет доступно по адресу: **http://localhost:8080**

### Сборка JAR

```bash
# Сборка
./gradlew bootJar

# Запуск JAR
java -jar build/libs/cleanshed-1.0.0.jar
```

### Запуск с настройками через переменные окружения

```bash
# Указать путь к базе данных
DB_PATH=/var/data/cleanshed.db ./gradlew bootRun

# Указать порт
PORT=3000 ./gradlew bootRun

# Все параметры вместе
DB_PATH=/var/data/cleanshed.db PORT=3000 ./gradlew bootRun
```

---

## Переменные окружения

| Переменная | Описание | Значение по умолчанию |
|-----------|----------|----------------------|
| `DB_PATH` | Путь к файлу базы данных SQLite | `clean-schedule.db` |
| `PORT` | Порт HTTP-сервера | `8080` |

---

## Структура проекта

```
cleanshed/
├── src/main/kotlin/click/ufate/cleanshed/
│   ├── CleanScheduleApplication.kt          # Точка входа
│   ├── config/
│   │   ├── SecurityConfig.kt                # Настройка Spring Security
│   │   └── WebConfig.kt                     # Маршруты ( forwards )
│   ├── controller/
│   │   ├── AdminController.kt               # API админки
│   │   ├── PersonController.kt              # API жильцов
│   │   ├── PlaceController.kt               # API мест
│   │   └── ScheduleController.kt            # API графика
│   ├── dto/
│   │   └── Dtos.kt                          # Объекты передачи данных
│   ├── entity/
│   │   ├── AdminConfig.kt                   # Конфигурация админа
│   │   ├── CleaningSchedule.kt              # Запись графика
│   │   ├── CleaningStats.kt                 # Статистика уборок
│   │   ├── Person.kt                        # Жилец
│   │   └── Place.kt                         # Место уборки
│   ├── repository/
│   │   ├── AdminConfigRepository.kt
│   │   ├── CleaningScheduleRepository.kt
│   │   ├── CleaningStatsRepository.kt
│   │   ├── PersonRepository.kt
│   │   └── PlaceRepository.kt
│   └── service/
│       ├── AdminService.kt                  # Аутентификация, пароли
│       ├── PersonService.kt                 #CRUD жильцов
│       ├── PlaceService.kt                  # CRUD мест
│       └── ScheduleService.kt               # Генерация и управление графиком
├── src/main/resources/
│   ├── application.yml                      # Конфигурация Spring
│   └── static/
│       ├── index.html                       # SPA (Vue.js)
│       ├── manifest.json                    # PWA манифест
│       ├── sw.js                            # Service Worker
│       └── icons/                           # Иконки PWA и apple-touch
├── build.gradle.kts                         # Скрипт сборки
└── settings.gradle.kts
```

---

## API Endpoints

### Жильцы

| Метод | URL | Описание |
|-------|-----|----------|
| `GET` | `/api/persons` | Список активных жильцов |
| `GET` | `/api/persons/{id}` | Жилец по ID |
| `GET` | `/api/persons/search?name=` | Поиск жильцов |
| `POST` | `/api/persons` | Добавить жильца |
| `PUT` | `/api/persons/{id}` | Обновить жильца |
| `DELETE` | `/api/persons/{id}` | Удалить жильца (деактивация) |
| `POST` | `/api/persons/{id}/allowed-places` | Добавить доступное место |
| `DELETE` | `/api/persons/{id}/allowed-places/{placeId}` | Убрать доступное место |

### Места

| Метод | URL | Описание |
|-------|-----|----------|
| `GET` | `/api/places` | Список активных мест |
| `GET` | `/api/places/{id}` | Место по ID |
| `GET` | `/api/places/common` | Только общие места |
| `GET` | `/api/places/search?name=` | Поиск мест |
| `POST` | `/api/places` | Добавить место |
| `PUT` | `/api/places/{id}` | Обновить место |
| `DELETE` | `/api/places/{id}` | Удалить место (деактивация) |

### График

| Метод | URL | Описание |
|-------|-----|----------|
| `GET` | `/api/schedule/today` | Задачи на сегодня |
| `GET` | `/api/schedule/week/{date}` | График на неделю |
| `GET` | `/api/schedule/all` | Весь график целиком |
| `POST` | `/api/schedule/generate` | Генерация графика (старый график удаляется полностью) |
| `PUT` | `/api/schedule/{id}/complete` | Отметить выполненным |
| `PUT` | `/api/schedule/{id}/incomplete` | Отменить выполнение |
| `GET` | `/api/schedule/stats` | Статистика уборок |
| `GET` | `/api/schedule/stats/summary` | Сводка статистики |

### Админ

| Метод | URL | Описание |
|-------|-----|----------|
| `POST` | `/api/admin/login` | Вход (пароль в теле запроса) |
| `POST` | `/api/admin/logout` | Выход |
| `GET` | `/api/admin/verify` | Проверка токена |
| `GET` | `/api/admin/password` | Получить мастер-пароль |
| `POST` | `/api/admin/reset-password` | Сбросить мастер-пароль |

---

## Страницы

| URL | Описание |
|-----|----------|
| `/` | Основная страница — сегодняшние задачи, график, статистика |
| `/admin` | Админ-панель — управление жильцами, местами, генерация графика |

---

## Как работает генерация графика

1. Админ выбирает день недели и количество недель
2. Система берёт всех активных жильцов и все активные места (личные + общие)
3. Старый график удаляется полностью, новый пишется с нуля
4. Для каждой недели, в две фазы:
   - **Фаза 1 — личные места:** каждое отдаётся своему владельцу
   - **Фаза 2 — общие места:** распределяются только среди свободных в этот день жильцов, поэтому «личное + общее» у одного человека не встречается
5. Честность обеспечивают штрафы: повтор места с прошлой недели (+1000), уже бывшая связка человек+место (+100 за каждый раз), общая нагрузка человека (+10 за задачу), занятость в этот день (+5). Из всех с минимальным штрафом выбирается случайный — повторные генерации различаются
6. Исключения, когда дубли неизбежны: мест больше, чем людей (тогда второе место дают тому, у кого сегодня нет личного, — «общее + общее»), или у личного места один владелец на несколько мест

---

## Лицензия

MIT
